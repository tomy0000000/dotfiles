name: Test Ubuntu features

on:
  push:
    branches: "**"
  pull_request:
    branches: "**"

jobs:
  Ubuntu:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-16.04, ubuntu-18.04, ubuntu-20.04]
    steps:
      - uses: actions/checkout@v2

      - name: Make All
        run: make
      - name: Essential
        run: make ubuntu-essential
      - name: Locale
        run: make locale-zhtw
      - name: Timezone
        run: make tz-taipei
      - name: SSH
        run: make ssh
      - name: stow
        run: make stow
      - name: Zsh
        run: make ubuntu-zsh
      - name: Dropbox
        run: make dropbox

      - name: Alias
        run: make alias
      - name: Configs
        run: make configs
      - name: Jupyter
        run: make jupyter
